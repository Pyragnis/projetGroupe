/*import { LOGIN_SUCCESS, LOGIN_FAILURE } from '../../actions/firebaseAuth';

const INITIAL_STATE = {
  user: null,
  error: '',
};

const authReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    case LOGIN_SUCCESS:
      return { ...state, user: action.payload, error: '' };
    case LOGIN_FAILURE:
      return { ...state, error: action.payload };
    default:
      return state;
  }
};

export default authReducer;
*/
  